import React, {
  Component,
  // PropTypes as Type,
} from 'react';

export default class PageNotFound extends Component {
  /**
   * Validates passed properties
   */
  static propTypes = {};

  /**
   * Renders 'PageNotFound' component
   */
  render() {
    return (
      <div className="c-page-not-found">NotFound</div>
    );
  }
}

